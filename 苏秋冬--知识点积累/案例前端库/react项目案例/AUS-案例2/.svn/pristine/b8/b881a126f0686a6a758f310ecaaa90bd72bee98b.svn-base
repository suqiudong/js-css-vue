import React, { Component } from 'react';
export default class Communal extends Component {
    constructor(props) {
        super(props);
    }
    render() {
        return <div className='Exhibition_select' data-name={this.props.title}>
            <h4>
                <span className="label label-primary Keyword">{this.props.title}</span>
            </h4>
            <ul className="list-group">
                {this.props.content}
            </ul>
        </div>
    }
}
