import createReducer from 'UTIL/createReducer'
import { ACTION_HANDLERS } from 'ACTION/dataSet'
import initState from 'STORE/initState'

export default createReducer(initState.dataset, ACTION_HANDLERS);
