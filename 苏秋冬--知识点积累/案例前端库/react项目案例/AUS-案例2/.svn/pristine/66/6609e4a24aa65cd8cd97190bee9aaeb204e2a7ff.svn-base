// ================================
// Action Type
// ================================
const CURR_MENU = 'CURR_MENU';

// ================================
// Action Creator
// ================================

const currMenu = (formData) => {
    return dispatch => formData
};
/* default 导出所有 Actions Creator */
export default {
    currMenu
}

// ================================
// Action handlers for Reducer
// ================================
export const ACTION_HANDLERS = {
    [CURR_MENU]: (payload) => payload
};
